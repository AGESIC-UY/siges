package com.sofis.business.ejbs;

import com.sofis.entities.constantes.ConstanteApp;
import java.util.concurrent.TimeUnit;
import java.util.logging.Logger;
import javax.ejb.AccessTimeout;
import javax.ejb.LocalBean;
import javax.ejb.Schedule;
import javax.ejb.Stateless;
import javax.inject.Inject;
import javax.inject.Named;
import javax.persistence.EntityManagerFactory;
import javax.persistence.PersistenceUnit;

/**
 *
 * @author Usuario
 */
@Named
@Stateless(name = "notificacionEnvioTimerBean")
@LocalBean
public class NotificacionEnvioTimerBean {
    
//    @PersistenceUnit(unitName = "JPAService")
//    private EntityManagerFactory emFactory;
    private static final Logger logger = Logger.getLogger(ConstanteApp.LOGGER_NAME);
    private static final String HORA_EJECUCION = "02";
    private static final String MINUTOS_EJECUCION = "00";
    
    @Inject
    private DatosUsuario du;
    @Inject
    private NotificacionEnvioBean notificacionEnvioBean;
    
//    @Schedule(hour = HORA_EJECUCION, minute = MINUTOS_EJECUCION)
//    @AccessTimeout(unit = TimeUnit.HOURS, value = 6)
//    public void scheduleEnvioNotificaciones() {
//        notificacionEnvioBean.enviarNotificaciones();
//    }
}
